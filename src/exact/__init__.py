from .transfer_matrix import (
    eigenvalues,
    partition_function,
    free_energy_per_spin,
    magnetization,
    magnetization_thermo_limit,
    nn_correlation,
    susceptibility,
    specific_heat,
    energy_per_spin,
    entropy_per_spin,
    compute_all,
    compute_grid,
)
